server:
  port: 30000

spring:
  application:
    name: api-geteway
zuul:
  routes:
    hello-service:
      path: /hello-service/**
      serviceId: hello-service

eureka:
  client:
    service-url:
      defaultZone: http://config.mokulive.stream:8080/eureka/