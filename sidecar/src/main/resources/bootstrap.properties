spring.application.name=sidecar

spring.profiles.active=dev

spring.cloud.config.uri=${config-server-uri:http://live-config.herokuapp.com}
spring.cloud.config.profile=${spring.profiles.active:dev}
spring.cloud.config.label=${config-server-label:master}

#spring.cloud.discovery.enabled=true
#spring.cloud.discovery.service-id=config-server
#spring.cloud.discovery.username=${config.username:test}
#spring.cloud.discovery.password=${config.username:test}
